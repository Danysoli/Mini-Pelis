<view class="movie-container">
  <text class="genre-title">{{genreName}}</text>
  <block a:for="{{movies}}" a:key="id">
    <movie-card id="{{item.id}}" title="{{item.title}}" poster="{{item.poster}}" mediaType="{{item.mediaType}}"></movie-card>
  </block>
  <navbar></navbar>
</view>